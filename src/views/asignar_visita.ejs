<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Asignacion de visitas</title>
    <link rel="stylesheet" href="css/style.css"/>
    <link rel="stylesheet" href="css/navbar.css"/>

</head>
<body>
    <%- include("components/navbar.ejs")%>

    <article>
        <div class="center">
            <form action="" method="post">
                <label for="asistentes">ID. Asistente social</label>
                <select name="asistentes" id="asistentes">
                    <option value="">Seleccione un asistente</option>
                    <% for(asistente of asistentes){ %>
                        <option value="<%= asistente['rut'] %>"><%= asistente['nombre'] %></option>
                    <% } %>
                </select>
                <label for="direhogar">ID. Visita</label>
                <select name="direhogar" id="direhogar">
                    <option value="">Seleccione una visita</option>
                    <% for(hogar of hogares){ %>
                        <option value="<%= hogar['id'] %>"><%= hogar["direccion"] %></option>
                    <% } %>
                </select>
                <label for="motivo">Motivo</label>
                <textarea name="motivo" id="" cols="30" rows="10"></textarea><br>
                </select>
                <button type="submit">Asignar</button>
            </form>
        </div>
    </article>
</body>
</html>